@model IEnumerable<FarmITApp.Models.CropType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CropTypeDescr)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MaturityDays)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CropTypeDescr)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MaturityDays)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CropTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CropTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CropTypeID })
        </td>
    </tr>
}

</table>
