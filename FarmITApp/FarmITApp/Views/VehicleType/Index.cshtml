@model IEnumerable<FarmITApp.Models.VehicleType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VehTypeDescr)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VehTypeImg)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VehTypeDescr)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VehTypeImg)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VehTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.VehTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VehTypeID })
        </td>
    </tr>
}

</table>
