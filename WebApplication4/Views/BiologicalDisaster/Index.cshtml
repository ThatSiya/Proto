@model IEnumerable<WebApplication4.Models.BiologicalDisaster>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BioDisasterDescr)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BioDisasterNotes)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BioDisasterDescr)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BioDisasterNotes)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BioDisasterID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BioDisasterID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BioDisasterID })
        </td>
    </tr>
}

</table>
