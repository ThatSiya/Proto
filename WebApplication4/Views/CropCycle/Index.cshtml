@model IEnumerable<WebApplication4.Models.CropCycle>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CropCycleStartDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CropCycleEndDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CropCycleDescr)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CropCycleStartDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CropCycleEndDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CropCycleDescr)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CropCycleID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CropCycleID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CropCycleID })
        </td>
    </tr>
}

</table>
